#!/bin/bash
# vim: set syntax bash

externalMonitor='HDMI-1-2'

xrandr | grep "$externalMonitor connected"
hasMultipleMonitors=$?

if [ $hasMultipleMonitors ]; then
    xrandr --output $externalMonitor --auto --right-of eDP-1-1
else
    xrandr --output $externalMonitor --off
fi
